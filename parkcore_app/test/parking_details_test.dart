import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:parkcore_app/parking/parking_details.dart';
import 'package:parkcore_app/parking/find_parking.dart';
import 'package:parkcore_app/parking/parking_details.dart';
import 'package:parkcore_app/parking/find_parking.dart';

void main() {
  
  /*testWidgets('Find Availability', (WidgetTester tester) async {
      // Build an app with a Text widget that displays the letter 'H'.
      Spot spot;
      await tester.pumpWidget(DetailScreen(spot: spot),
      );

      // Find a widget that displays the letter 'H'.
      expect(find.byType(RaisedButton), findsOneWidget);
  });
  testWidgets('Find Size', (WidgetTester tester) async {
      // Build an app with a Text widget that displays the letter 'H'.
      Spot spot;
      await tester.pumpWidget(DetailScreen(spot: spot),
      );

      // Find a widget that displays the letter 'H'.
      expect(find.text('Size:'), findsOneWidget);
  });*/
}